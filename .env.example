# Copy to .env and adjust. .env is gitignored.

# MongoDB connection string used by server.py and scripts/.
MONGODB_URI=mongodb://localhost:27017

# Optional: tags every MCP tool-call log entry in db.runs.
# scripts/run_session.sh (added in Phase 2) sets this automatically per session.
SESSION_ID=default
